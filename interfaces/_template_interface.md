# インターフェース定義テンプレート
<!-- このファイルを interfaces/[ドメイン名]_interface.md にコピーして使用する -->
<!-- ここにはAPIの「外から見える仕様」だけを書く。内部実装は書かない。 -->
<!-- orchestrate スキルがドメイン分割時に自動生成する -->

## [DOMAIN_NAME] サービス インターフェース

### バージョン
| 項目 | 値 |
|------|-----|
| IF バージョン | 1 |
| 最終更新 | YYYY-MM-DD |
| 担当ドメイン | [domain] |

### 提供するエンドポイント
| メソッド | パス | 概要 | 対応Spec |
|---------|------|------|---------|
| POST | /api/[domain]/... | ... | REQ-XXX-NNN |

### 提供する型定義
```typescript
// 他ドメインが参照してよい型のみ記載
export type [TypeName] = {
  id: string;
  // ...
}
```

### イベント（非同期連携がある場合）
| イベント名 | 発火タイミング | ペイロード | 消費者ドメイン |
|-----------|--------------|-----------|-------------|
| [domain].created | ... | ... | [consumer] |

### 利用する他ドメインのIF
<!-- このドメインが依存する他ドメインのIFを列挙 -->
<!-- 循環依存に注意: A→B→A は禁止。イベント経由の間接連携で解消する -->
| 依存先ドメイン | 利用するエンドポイント/型 | 用途 |
|-------------|----------------------|------|
| なし | - | - |

### 破壊的変更ルール
- エンドポイントの削除・パス変更・レスポンス型の変更は**破壊的変更**とみなす
- 破壊的変更を行う場合は、消費者ドメインのIF定義も同時に更新すること
- IFバージョンを+1し、PRの説明に変更内容を記録する
