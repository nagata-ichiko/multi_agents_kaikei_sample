# Output Contract: Impact Report（影響範囲レポート）

## 保存先
`docs/impact-reports/[Spec ID].md`

## フォーマット

```markdown
# 影響範囲レポート: [REQ-ID]

## 影響範囲

### 読取ファイル一覧（coderが実装中に読んだファイル）
- path/to/file.ts（目的: XXX）

### 変更ファイル一覧
| ファイル | 操作 | 概要 |
|---------|------|------|
| src/auth/signup.ts | 新規 | サインアップAPI |
| src/auth/signup.test.ts | 新規 | テスト |

### 新規追加した関数・エンドポイント
- `POST /api/auth/signup` — ユーザー登録

### 変更した既存の関数・エンドポイント
- `src/models/user.ts:updateProfile()` — バリデーション追加

### 共通化の影響（該当する場合）
- shared-components.md に `AuthForm` を追記

### 副作用の可能性
- なし / あれば記載

### テスト観点
- 正常系: [テストケース]
- 異常系: [テストケース]

## 設計判断と根拠

### アーキテクチャ上の判断
| 判断内容 | 選択肢 | 採用理由 |
|---------|--------|---------|

### 仮採用した設計判断（設計情報不足時）
| 不足情報 | 仮採用した判断 | 根拠 |
|---------|--------------|------|

## レビュー結果サマリ

### 修正済みの指摘（信頼度80以上）
| finding_id | 深刻度 | 概要 | イテレーション |

### 参考情報として記録した指摘（信頼度80未満）
| finding_id | 深刻度 | 概要 | 信頼度 |
```
