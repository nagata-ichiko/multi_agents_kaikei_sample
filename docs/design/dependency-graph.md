# Spec 依存グラフ

<!-- AI: spec-all / draft-spec / revise-spec 実行時にこのファイルを自動更新すること -->
<!-- 手動編集不要。スキルが自動生成する -->

## 依存関係図

```mermaid
graph LR
    REQ-OVERVIEW-001[OVERVIEW]
    %% AI: ここにSpec間の依存関係を自動生成する
    %% 例:
    %% REQ-AUTH-001[認証] --> REQ-USER-001[ユーザー管理]
    %% REQ-ORDER-001[注文] --> REQ-AUTH-001[認証]
    %% REQ-ORDER-001[注文] --> REQ-PRODUCT-001[商品]
```

## 依存関係マトリクス

<!-- AI: Spec数が10以上の場合はマトリクス形式でも表示する -->

| Spec ID | 依存先（このSpecが使う） | 被依存先（このSpecを使う） |
|---------|------------------------|--------------------------|
| REQ-OVERVIEW-001 | なし | 全Spec |

## 循環依存の検出

<!-- AI: 循環依存が検出された場合はここに警告を記載する -->

現時点で循環依存は検出されていません。

## 変更影響分析

<!-- AI: revise-spec 実行時に、変更対象のSpecから被依存先を辿り、影響範囲を記載する -->
<!-- 形式: REQ-XXX-NNN を変更 → 影響を受けるSpec一覧 -->
